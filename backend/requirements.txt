# ── Web framework ─────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9        # file uploads

# ── Database ──────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0                # async PostgreSQL driver
alembic>=1.13.0                # migrations (optional but recommended)

# ── Data processing ───────────────────────────────────────────────────────
pandas>=2.2.0
openpyxl>=3.1.0                # Excel read/write
numpy>=1.24.0
scipy>=1.11.0

# ── OCR / document parsing (PP-StructureV3) ────────────────────────────────
# NOTE: requires a CPU with AVX/AVX2 support (Celeron/Atom-class CPUs from the
# Gemini Lake / Goldmont family and similar will fail with a DLL init error).
# Pinned to the exact versions confirmed working — avoid bumping casually,
# since paddlex/paddlepaddle version mismatches are a common breakage source.
paddlepaddle==3.3.1
paddlex[ocr]==3.7.2

# For GPU machines instead of CPU, swap the line above for:
# paddlepaddle-gpu==3.3.1

# PDF support
pymupdf>=1.24.0                # PyMuPDF — converts PDF pages to images

# ── Validation & typing ───────────────────────────────────────────────────
pydantic>=2.7.0
email-validator>=2.1.0

# ── Utilities ─────────────────────────────────────────────────────────────
python-dateutil>=2.8.2
Pillow>=10.0.0